# Assemble MVP System Design Simulator RFC

Type: task
Status: resolved
Blocked by: 08, 09, 10, 11

## Question

Write the RFC artifact `rfc.md` at the **repo root**, implementing all resolved RFC decisions and referencing [[prd]]. Enough technical direction to start implementation tickets — not the implementation itself.

## Answer

RFC assembled at repo root: [`rfc.md`](../../../rfc.md)

Locks Vite+React+TS, `@xyflow/react`, domain + pure simulation engines, Zustand-leaning store, localStorage `sds.diagram.v1` + JSON import/export, folder layout, and suggested build sequence.
